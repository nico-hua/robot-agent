# 项目开发规范

本仓库当前围绕统一 Provider 配置、OpenAI/Anthropic 兼容适配器和通用工具基础设施重建。当前源码包含配置加载、Provider/消息接口、工具注册与发现基础；不应把尚未验证的端到端调用、Agent Runtime、自动编排或具体业务能力视为已经实现。

## 项目原则

- 修改前先阅读相关文件，并理解已有约束与上下文。
- 保持模块职责清晰，避免职责混杂。
- 避免不必要的抽象，优先采用简单、可测试的实现。
- 不引入当前阶段不需要的依赖。
- 不为了未来可能的需求提前设计复杂架构。
- 不修改任务范围之外的文件。
- 不删除用户已有内容；遇到冲突时先说明并确认处理方式。
- 所有重要行为都应有测试。
- 配置、代码和文档必须保持一致。

## Python 规范

- 使用 Python 3.10（含）至 3.13（不含）。
- 使用 `pyproject.toml` 管理项目配置。
- 使用 `uv` 管理依赖和虚拟环境。
- 为公开接口及复杂内部逻辑使用明确的类型标注。
- 保持函数和模块职责单一，避免过长函数。
- 避免隐式全局状态。
- 避免在导入阶段执行副作用操作。
- 使用清晰、稳定的异常处理；不要吞没异常或依赖不明确的失败行为。
- 日志不得泄露密钥、令牌、个人信息或其他敏感信息。

## 测试规范

- 测试放在 `tests/` 目录。
- 测试应尽量相互独立，不依赖执行顺序或外部残留状态。
- 外部服务和设备必须通过 Mock 或 Fake 隔离。
- 新增功能必须同步增加测试。
- 测试失败时应优先定位根因，不要简单跳过、屏蔽或弱化测试。

## 文档规范

- README 只描述当前真实状态。
- 尚未实现的功能必须明确标注为未实现。
- 设计决策应记录其原因，而不只记录结论。
- 不要把计划功能描述成已经完成。
- 代码、配置和文档中的名称必须保持一致。

## 推荐命令

在适用时使用以下命令：

```powershell
uv sync
uv run pytest
uv run ruff check .
uv run ruff format --check .
```

如果某个命令因尚未添加所需依赖而无法执行，应说明原因；不要为此引入与当前任务无关的大量依赖。
